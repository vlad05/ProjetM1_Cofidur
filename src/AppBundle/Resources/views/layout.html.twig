{% extends 'AppBundle::base.html.twig' %}

{% block sidebar_nav %}
    {# Pages accessibles par tout le monde #}
    <h2><a href="{{ path('AppBundle_about') }}">A propos</a></h2>
    {# Pages accessibles par les utilisateurs#}
    {% if is_granted('ROLE_USER') %}
    <h2><a href="/profile">Profil</a></h2>
    {% endif %}
    {# Pages accessibles par les administrateurs #}
    {% if is_granted('ROLE_ADMIN') %}
    <h2><a href="{{ path('AppBundle_admin') }}">Administration</a></h2>
    {% endif %}
{% endblock sidebar_nav %}
